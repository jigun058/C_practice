List Sorted;
    // init(&Sorted);

    // insert(&Sorted, 0, delete(&L, 0));

    // count = 0;
    // while(count < 10){
    //     int temp = delete(&L, 0);
    //     for(int i=0; i<10; i++){
    //         if(temp < getEntry(&Sorted, i)){
    //             insert(&Sorted, i, temp);
    //             count += 1;
    //             break;
    //         }
    //         if(i==count){
    //             insert(&Sorted, i, count);
    //         }
    //     }
    // }

    // for(int i=0; i<10; i++)
    //     printf("%d\n", delete(&Sorted, 0));
